## 新增私有欄位鑑識摘要

### 目的

正式 Actions 仍有 7 筆價格衝突，但公開 log 只有欄位名稱，無法判斷它們是否為買賣估算價或非交易型資料。這個 PR 先補足私有、無金額的鑑識摘要，不放行任何資料。

### 新增摘要

- 價格唯一差異筆數
- 舊版混合表單候選筆數與其中非買賣交易筆數
- 前後動作類別配對（BUY／SELL／SET_BALANCE 等）
- 前後價格來源（derived／explicit）配對

摘要不包含金額、標的、數量、交易 ID 或完整 payload；完整資料仍只保留在私有 Supabase／私有 artifact。

### 安全性

- 不修改 immutable ledger。
- 不放寬任何 conflict 判定。
- 真正核心欄位差異仍隔離。

### 驗證

- `python -m pytest -q`：211 passed。
- 新增非交易型 legacy 價格衝突的分類測試，確認不會被誤判為估算價格重播。

合併並執行一次正式 Actions 後，依摘要結果建立下一個只針對實際資料型態的修復 PR。
