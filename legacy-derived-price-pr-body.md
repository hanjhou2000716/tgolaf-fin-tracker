## 修復舊版混合表單的估算價格重播

### 問題

正式 Actions 已將 immutable ledger 異常由 29 筆縮減至 7 筆。鑑識摘要顯示剩餘項目只有 `price` 不同，且前後都帶有 `legacy_mixed_form_row`。這是舊版混合表單買賣事件缺少成交價，後續以結算行情補價時保留舊相容標記所造成的重播，不是新的核心交易變更。

### 修復

- 僅對舊版混合表單的 BUY／SELL 事件啟用價格重播判定。
- 必須確認唯一變更欄位是 `price`，且前後都有價格；標的、動作、數量、日期、幣別與單位仍嚴格比對。
- 將安全重播分類為 `REPLAY_DERIVED_PRICE`，保留既有 Supabase immutable ledger，不覆寫或刪除歷史資料。
- 來源列或交易 ID 改變時，也可透過不含價格的舊版核心指紋辨識同一事件。

### 驗證

- `python -m pytest -q`：209 passed。
- 新增測試：舊版混合表單估算價格變動可安全重播；數量等核心欄位變更仍為 `CONFLICT`。

### 驗收與回滾

合併後重跑一次正式 Actions，確認 7 筆降為估算價格重播且不再產生資料異常告警。若鑑識發現非價格欄位差異，仍會維持隔離。撤回本 PR 即可恢復原本嚴格判定。
