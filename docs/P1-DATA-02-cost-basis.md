# P1-DATA-02 成本與已實現損益

`cost_basis.py` 提供可重建、可測試的交易成本引擎。台股預設採移動平均成本；美股可設定 FIFO 或平均成本。手續費與交易稅計入成本及已實現損益，未實現損益則以指定行情快照計算。

此模組只接受已通過 `transaction_schema.Transaction` 驗證的 BUY/SELL 事件；缺少價格、負數數量或賣出超過持倉會直接拒絕，不會默默忽略。
