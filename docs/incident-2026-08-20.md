# Production ingestion incident — 2026-08-20

The failed production run was `32340912399` at main SHA
`3451a041becc12455482058d622bc16167031582`. The tracker failed in the
`Run Tracker Script (Generate private snapshot and public Demo)` step with:

```text
RuntimeError: immutable ledger conflict for transaction_id
39c68de3-381d-5ecf-bdc8-5920d51676b5
```

The failure occurred after Form parsing, while synchronizing a UUID whose
stored immutable payload differed from the current derived payload. It was not
a market quote, public sanitizer, or Pages artifact failure. The production
fixture is kept at
`tests/fixtures/production-immutable-conflict-20260820.json`.

The repair quarantines that row with reason `immutable_ledger_conflict`, keeps
the existing ledger row untouched, continues valid rows, and exposes the
degraded reason through the private Data Health and Telegram contracts.
